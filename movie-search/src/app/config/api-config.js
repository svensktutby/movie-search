import environment from '../../../environment';

// eslint-disable-next-line no-unused-vars
const { key } = environment;

const config = {
  origin: 'https://www.omdbapi.com/',
  // apikey: `apikey=${key}`,
  apikey: 'apikey=8618d2a5',
};

export default config;
